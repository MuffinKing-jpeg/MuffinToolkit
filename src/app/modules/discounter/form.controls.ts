import {FormControl} from '@angular/forms';

export const valuesControls = {
  startValue: new FormControl(0),
  endValue: new FormControl(0),
  percent: new FormControl(0),
  absoluteValue: new FormControl(0),
}
